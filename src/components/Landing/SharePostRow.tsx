import {StyleSheet, Text, SafeAreaView} from 'react-native';
import React from 'react';

export const SharePostRow = () => {
  return (
    <SafeAreaView>
      <Text>SharePostRow</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});
